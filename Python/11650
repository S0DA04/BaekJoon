import sys;
N = int(sys.stdin.readline());
Unsorted = [];
Sorted = [];
for i in range(N):
  x,y = map(int, sys.stdin.readline().split());
  Unsorted.append([x,y]);
Unsorted.sort(key = lambda x:(x[0],x[1]));
Sorted = Unsorted;
for i in Sorted:
  for j in i:
    print(j, end=" ");
  print()
